#!/usr/bin/env sh
# Dummy gradlew - real wrapper jar ke liye Android Studio use karo
# Ya: gradle wrapper --gradle-version 8.4

echo "Use Android Studio to build, or install gradle:"
echo "gradle wrapper --gradle-version 8.4"
echo "Then: ./gradlew assembleDebug"
